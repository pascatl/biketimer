// ── Application identity ──────────────────────────────────────────────────────
// Set VITE_APP_NAME in your .env to rename the application everywhere in the UI
export const APP_NAME = import.meta.env.VITE_APP_NAME || "BikeTimer";
